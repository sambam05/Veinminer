# Fabric Version Folder Notes

This directory contains migration placeholder entrypoint classes by Minecraft version.

Status:

- Not part of current active build wiring.
- Not referenced by current wrapper module `sourceSets`.
- Kept to preserve planned target structure for future migration.

Active Fabric wrapper modules are currently the top-level `VMM-fabric-v*` projects.
